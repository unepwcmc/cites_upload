$(function(){
  $("#options_<%=@user.id %>").html("<%= escape_javascript(link_to "Approve", approve_user_path(@user), :method => :put, :remote => true, :class => "btn success") %>");
  $("#waiting").find('tbody').append($("#user_<%=@user.id%>"));
});

